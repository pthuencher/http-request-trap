extends layout.pug 

block content
    ul.nav.nav-tabs
        li.nav-item 
            a.nav-link(href="/?page=trap") Trap   
        li.nav-item 
            a.nav-link.active(href="/?page=redirect") Redirect
        li.nav-item 
            a.nav-link(href="/?page=serve") Serve

    form.settings(action="/settings", method="POST")
        br
        input.btn.btn-success(type="submit", value="Save Changes")
        br
        br
        strong Redirect to (via /redirect): 
        br
        input(type="input", name="redirect_url", value=locals.redirect_url)